                          <th className="px-6 py-4 text-center text-sm font-bold text-gray-900 dark:text-gray-100">Average</th>
                          <th className="px-6 py-4 text-center text-sm font-bold text-gray-900 dark:text-gray-100">Grade</th>
                        </tr>
                      </thead>
                      <tbody className="divide-y divide-gray-200 dark:divide-gray-700">
                        {students.map((student, i) => (
                          <tr key={i} className="hover:bg-gray-50 dark:hover:bg-gray-700">
                            <td className="px-6 py-4 font-semibold text-gray-900 dark:text-gray-100">{student.name}</td>
                            <td className="px-6 py-4 text-center text-gray-900 dark:text-gray-100">{student.rollNo}</td>
                            <td className="px-6 py-4 text-center">
                              <span className="inline-flex px-3 py-1 rounded-full text-sm font-bold bg-blue-100 text-blue-700 dark:bg-blue-900/30 dark:text-blue-400">
                                {[83, 76, 89][i] || 80}%
                              </span>
                            </td>
                            <td className="px-6 py-4 text-center">
                              <span className="inline-flex px-3 py-1 rounded-full text-sm font-bold bg-green-100 text-green-700 dark:bg-green-900/30 dark:text-green-400">
                                {[83, 76, 89][i] >= 80 ? 'A' : 'B'}
                              </span>
                            </td>
                          </tr>
                        ))}
                      </tbody>
                    </table>
                  </div>
                </div>
              </div>
            )}
          </div>
        )}
